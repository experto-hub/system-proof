package io.github.jacekkardys.systemproof.proof;

/** Closed terminal outcome for one explicitly controlled proof execution. */
public enum ProofOutcome {
    PROVED,
    VIOLATED,
    INCONCLUSIVE,
    ERROR
}
